const arr = [1, 2, 999, 56, "Nishant", "125", "PW Student"];

for (let index = 0; index < arr.length; index++) {
    if (typeof arr[i] === "string") {
      console.log(`Found The First String: ${arr[index]}`);
      break;
    }
  }